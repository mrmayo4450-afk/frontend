# When running the frontend on a separate server from the backend,
# set this to your backend's full URL (no trailing slash).
# Leave blank if frontend and backend are on the same server.
VITE_API_URL=https://your-backend-domain.com
